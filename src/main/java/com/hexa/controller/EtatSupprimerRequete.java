package com.hexa.controller;

import com.hexa.view.Window;
import com.hexa.controller.EtatAuMoinsUneRequete;

public class EtatSupprimerRequete implements State {

  // public void entryAction(Controller c, Windiw w) {
  // w.activerBoutonsSupprimer();
  // }
  //
  // public void clicDroit(Controller c, Window w) {
  // c.setCurrentState(c.etatAuMoinsUneRequete);
  // }
  //
  // public void clicGauche(Controller c, Window w, Coordonnees mouseCoord) {
  // foreach(Livraison l: c.getTournee().getLivraisons) {
  // Coordonnees posLivraison =
  // w.getGraphicalView().CoordGPSToViewPos(l.getIntersection());
  // if (mouseCoord.equals(posLivraison)) {
  //
  // }
  // }
  // }
}
